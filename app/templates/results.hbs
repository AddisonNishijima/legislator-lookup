<div id="results">
  <ul>
    {{#each model as |legislator|}}
      <li> <h3>{{legislator.first_name}} {{legislator.last_name}}</h3>Contact: <a href=  {{legislator.contact_form}}>{{legislator.contact_form}}</a></li>
    {{/each}}
  </ul>
</div>
